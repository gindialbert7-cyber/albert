# Supabase project configuration
# Run `supabase init` then `supabase link --project-ref YOUR_PROJECT_REF`

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323

[inbucket]
enabled = true
port = 54324

[storage]
enabled = true
file_size_limit = "50MiB"

[auth]
enabled = true
site_url = "albert://auth"
additional_redirect_urls = ["https://albert-app.com", "albert://auth"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
enable_signup = true
enable_anonymous_sign_ins = false
minimum_password_length = 8

[auth.email]
enable_signup = true
double_confirm_changes = true
enable_confirmations = false   # flip to true before production

[auth.external.google]
enabled = false   # set to true + add client_id/secret for Google OAuth

# Edge functions are defined under [functions.fn-name]
# e.g. [functions.my-function]
